version: "2"
sql:
  - name: "links"
    engine: postgresql
    schema: "db/migrations"
    queries: "db/query"
    gen:
      go:
        package: "linksdb"
        out: "internal/db/links"
        emit_json_tags: true
        emit_interface: true
        sql_package: "pgx/v5"